steps:

- task: NuGetToolInstaller@1

- task: NuGetCommand@2
  inputs:
    restoreSolution: '**/*.sln'

- task: DownloadSecureFile@1
  name: T4File
  inputs:
    secureFile: '${{ parameters.flavour }}.prod.cs'

- task: DownloadSecureFile@1
  name: GoogleFile
  inputs:
    secureFile: 'google-services.json'

- task: Bash@3
  env:
    FLAVOUR: ${{ parameters.flavour }}
    PACKAGE: ${{ parameters.package }}
    APP_NAME: ${{ parameters.app_name }}
  inputs:
    filePath: 'Bootleg.Android/appcenter-pre-build.sh'

- task: XamarinAndroid@1
  inputs:
    projectFile: '**/Bootleg.Android.csproj'
    outputDirectory: '$(outputDirectory)'
    configuration: '$(buildConfiguration)'

- task: CopyFiles@2
  inputs:
    contents: '**/*.apk'
    targetFolder: $(Build.ArtifactStagingDirectory)

# - task: Bash@3
#   env:
#     FLAVOUR: ${{ parameters.flavour }}
#   inputs:
#     targetType: 'inline'
#     script: cp $(Build.ArtifactStagingDirectory)/Bootleg.Android.apk $(Build.ArtifactStagingDirectory)/${FLAVOUR}.apk 

- publish: $(Build.ArtifactStagingDirectory)
  artifact: ${{ parameters.flavour }}
